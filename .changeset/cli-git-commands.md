---
"@devix/cli": minor
---

Adds read-only `devix git` commands: `status` (branch and changed paths with short codes), `branches` (local branches with the current one marked) and `diff` (per-file line counts against HEAD), all with `--cwd` and clear one-line errors outside repositories.
